import { moduleFor, test } from 'ember-qunit';

moduleFor('view:sort-by-grouper', 'Unit | View | sort by grouper');

// Replace this with your real tests.
test('it exists', function(assert) {
  var view = this.subject();
  assert.ok(view);
});
