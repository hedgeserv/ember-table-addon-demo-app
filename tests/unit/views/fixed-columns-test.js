import { moduleFor, test } from 'ember-qunit';

moduleFor('view:fixed-columns', 'Unit | View | fixed columns');

// Replace this with your real tests.
test('it exists', function(assert) {
  var view = this.subject();
  assert.ok(view);
});
